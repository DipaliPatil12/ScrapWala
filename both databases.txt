show databases;

create database Scrapwala;

show databases;

use scrapwala;

show tables;

create table admins (admin_id integer primary key, name varchar(40)); 

create table users (user_id integer primary key, name varchar(40), password varchar(30), mobile_number varchar(40), 
address varchar(60), pincode varchar(30), date date); 

create table papers (paper_id integer primary key, user_id integer, Foreign key (user_id) references users(user_id), 
name varchar(40), rate integer, weight integer);

create table palastics (plastic_id integer primary key, user_id integer, Foreign key (user_id) references users(user_id), 
name varchar(40), rate integer, quantity integer);
drop table palastics;

create table plastics (plastic_id integer primary key, user_id integer, Foreign key (user_id) references users(user_id), 
name varchar(40), rate integer, quantity integer);

create table ElectricAppliances (electricappliance_id integer primary key, user_id integer, Foreign key (user_id) references users(user_id), 
name varchar(40), rate integer, quantity integer);

create table ItAppliances (itappliance_id integer primary key, user_id integer, Foreign key (user_id) references users(user_id), 
name varchar(40), rate integer, quantity integer);

create table Metals (metal_id integer primary key, user_id integer, Foreign key (user_id) references users(user_id), 
name varchar(40), rate integer, weight integer);

create table Textiles (textile_id integer primary key, user_id integer, Foreign key (user_id) references users(user_id), 
name varchar(40), rate integer, quantity integer);

create table Others (other_id integer primary key, user_id integer, Foreign key (user_id) references users(user_id), 
name varchar(40), rate integer, quantity integer, weight integer);

create database Organization;
use organization;
create table Employees (employee_id integer primary key, name varchar(50), contact varchar(40));   

show tables;

use scrapwala;

create table Pickups (pickup_id integer primary key, user_id integer, Foreign key (user_id) references users(user_id), 
name varchar(40), rate integer, quantity integer, weight integer);
 
create table statuses (status_id integer primary key, employee_id integer, Foreign key (employee_id) references employees(employee_id), isAvailable tinyint); 
use scrapwala;
use organization;
drop table orders;
use scrapwala;

drop table pickups;
use scrapwala;

create table Pickups (pickup_id integer primary key, product_id integer, Foreign key (product_id) references products(product_id), 
date date, time datetime, pickup_address varchar(40), remarks varchar(40));

create table Products (product_id integer primary key, electricappliance_id
integer, Foreign key (electricappliance_id) references 
electricappliances(electricappliance_id), itappliance_id integer, Foreign key (itappliance_id) references 
itappliances(itappliance_id), metal_id integer, Foreign key (metal_id) references 
metals(metal_id), other_id integer, Foreign key (other_id) references others(other_id), 
paper_id integer, Foreign key (paper_id) references papers(paper_id), 
plastic_id integer, Foreign key (plastic_id) references plastics(plastic_id), 
textile_id integer, Foreign key (textile_id) references textiles(textile_id));

drop table products;
use organization;
create table orders (order_id integer primary key, pickup_id integer, foreign key (pickup_id) references scrapwala.pickups(pickup_id)); 

create table meetings (meeting_id integer primary key, order_id integer, Foreign key (order_id) references orders(order_id), 
status_id integer, Foreign key (status_id) references statuses(status_id)); 





